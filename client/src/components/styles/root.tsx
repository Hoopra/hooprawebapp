import styled from 'styled-components';

export const StyleBody = styled.div`

  * {
    font-family: 'Niramit', sans-serif;
  }

  .button-collapse {
    cursor: pointer;
  }

  .link {
    cursor: pointer;
    color: #039be5;
  }
`;
