// const { BASE_DIR, executeCommand } = require('./dev_methods');

// const args = process.argv.slice(2);

// if (args.includes('ui')) {
//   executeCommand('npm run build:ui', `${BASE_DIR}/frontend`);
// }
// if (args.includes('admin')) {
//   executeCommand('npm run build:docker:admin', `${BASE_DIR}/frontend`);
// }
// if (args.includes('viewer')) {
//   executeCommand('npm run build:docker:viewer', `${BASE_DIR}/frontend`);
// }
// if (args.includes('translations')) {
//   executeCommand('npm run extract-translations', `${BASE_DIR}/frontend`);
// }
