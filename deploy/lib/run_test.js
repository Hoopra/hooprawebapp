// const { BASE_DIR, executeCommand } = require('./dev_methods');

// const args = process.argv.slice(2);
// const mapping = {
//   lib: '/lib',
//   api: '/api',
//   ui: '/ui',
//   workers: '/workers'
// };

// for (const arg of args) {
//   const path = mapping[arg];
//   if (path) {
//     executeCommand('npm run test', `${BASE_DIR}${mapping[arg]}`);
//   }
// }
